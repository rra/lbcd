# Lintian incorrectly thinks that update-rc.d <service> disable calls
# duplicate the calls added to set up the init script.  These calls port
# the old NO_START configuration to a properly disabled daemon.
lbcd: duplicate-updaterc.d-calls-in-postinst lbcd
