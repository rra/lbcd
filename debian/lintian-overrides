# Lintian incorrectly thinks that update-rc.d <service> defaults-disabled
# call duplicates the call added to set up the init script in the default
# case.  These calls port the old NO_START configuration to a properly
# disabled daemon.
lbcd: duplicate-updaterc.d-calls-in-postinst lbcd
